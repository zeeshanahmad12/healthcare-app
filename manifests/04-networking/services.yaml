apiVersion: v1
kind: Service
metadata:
  name: healthcare-internal
  namespace: healthcare
spec:
  clusterIP: None
  selector:
    app: healthcare-app
  ports:
  - port: 8080
    name: web
---
apiVersion: v1
kind: Service
metadata:
  name: healthcare-service
  namespace: healthcare
spec:
  selector:
    app: healthcare-app
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
